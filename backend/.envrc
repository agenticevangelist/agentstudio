# Auto-activate local virtualenv when entering this directory (requires direnv)
# If you do not use direnv, see README.md for a zsh fallback snippet.
if [ -d .venv ]; then
  source .venv/bin/activate
fi
